name: easy_geofencing
description: This is a dart package which give the best & optimized geofence background services features for both android & ios. Updated for Flutter 3.x and modern Android versions.
version: 1.0.0
repository: https://github.com/jay-simformsolutions/easy_geofencing

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
